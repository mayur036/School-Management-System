import { Building2, ShieldCheck, User, UserCheck } from 'lucide-react';

export const GUEST = {
  LOGIN: User,
  BUILDING: Building2,
  SECURITY: ShieldCheck,
  USER_CHECK: UserCheck,
};

export default GUEST;
