import PagePlaceholder from '@/components/shared/PagePlaceholder';
import { STAFF } from '@/lib/icons';

export const StaffDashboard = () => (
  <PagePlaceholder title="Dashboard" Icon={STAFF.HOME} />
);

export default StaffDashboard;
