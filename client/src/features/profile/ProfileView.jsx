import { UserCog } from 'lucide-react';

import PagePlaceholder from '@/components/shared/PagePlaceholder';

export const ProfileView = () => (
  <PagePlaceholder title="Account Settings" Icon={UserCog} />
);

export default ProfileView;
