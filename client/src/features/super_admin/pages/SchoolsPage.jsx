import PagePlaceholder from '@/components/shared/PagePlaceholder';
import { SUPER_ADMIN } from '@/lib/icons';

export const SchoolsPage = () => (
  <PagePlaceholder title="Schools" Icon={SUPER_ADMIN.SCHOOLS} />
);

export default SchoolsPage;
